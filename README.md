## My Personal Website

ssg, works without client javascript enabled

## develop

the usual `npm run dev`

## deploy

`npm run build`
result in a static files in `dist` folder, because its just html and css you can deploy it anywhere.
